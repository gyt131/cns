import java.security.MessageDigest;
import java.security.NoSuchAlgorithmException;
import java.util.Scanner;

public class SHA1Example {

    public static void main(String[] args) {

        try {

            Scanner sc = new Scanner(System.in);

            System.out.print("Enter the message: ");
            String message = sc.nextLine();

            // Create SHA-1 MessageDigest object
            MessageDigest md = MessageDigest.getInstance("SHA-1");

            // Convert message to byte array
            md.update(message.getBytes());

            // Perform hashing
            byte[] digest = md.digest();

            // Convert byte array into hexadecimal format
            StringBuffer hexString = new StringBuffer();

            for (int i = 0; i < digest.length; i++) {
                hexString.append(Integer.toHexString(0xff & digest[i]));
            }

            System.out.println("Message Digest (SHA-1): " + hexString.toString());

        } catch (NoSuchAlgorithmException e) {
            System.out.println("SHA-1 Algorithm not found");
        }
    }
}
 
Input & Output:
Enter the message: Hello World
Message Digest (SHA-1): 2ef7bde608ce5404e97d5f042f95f89f1c232871
